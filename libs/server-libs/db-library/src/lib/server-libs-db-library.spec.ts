import { serverLibsDbLibrary } from './server-libs-db-library';

describe('serverLibsDbLibrary', () => {
  it('should work', () => {
    expect(serverLibsDbLibrary()).toEqual('server-libs-db-library');
  });
});
